********
crepel.c
********
The crepel.c must be compiled so it can be imported into a python file.
To compile it, run:

python setup.py install

This will create something like "crepel.cp35-win_amd64.pyd". 
Now "crepel" can be imported and used. 